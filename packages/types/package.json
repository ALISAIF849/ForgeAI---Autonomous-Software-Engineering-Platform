{
  "name": "@forgeai/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types not covered by @forgeai/sdk — UI-only unions, client-side enums. If it's derivable from the OpenAPI schema, it belongs in @forgeai/sdk instead, not here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@forgeai/config": "workspace:*"
  }
}

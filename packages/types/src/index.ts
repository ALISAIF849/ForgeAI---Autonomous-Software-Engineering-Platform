// Placeholder entry point so this package is valid TypeScript and `main`/`types` in
// package.json resolve to a real file. No shared types yet — see package.json's
// description for the boundary between this package and @forgeai/sdk.
export {};

// Placeholder entry point so this package is valid TypeScript and `main`/`types` in
// package.json resolve to a real file. No components yet — see package.json's
// description and docs/architecture/09-frontend-architecture.md §6.
export {};

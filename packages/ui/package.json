{
  "name": "@forgeai/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared shadcn/ui-based design system — the mechanism behind \"must never look like ChatGPT\" (docs/architecture/09-frontend-architecture.md §6). No components yet.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@forgeai/types": "workspace:*"
  },
  "devDependencies": {
    "@forgeai/config": "workspace:*"
  }
}

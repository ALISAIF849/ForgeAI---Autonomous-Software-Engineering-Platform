// Placeholder entry point so this package is valid TypeScript and `main`/`types` in
// package.json resolve to a real file. Real content is generated (see package.json's
// "generate" script and description) — nothing to hand-write here, ever.
export {};

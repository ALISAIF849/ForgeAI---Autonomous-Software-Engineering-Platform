{
  "name": "@forgeai/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript base configuration consumed by every other TS package. Not a place for logic.",
  "files": [
    "tsconfig.base.json"
  ]
}
